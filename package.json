{
  "name": "aitrust-agent-starter-ts-blank-claude-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Insurance claims AI agent starter for integrating KOBIL Agent Trust with the Claude Agent SDK",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "dev": "node --import tsx src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.237",
    "@anthropic-ai/sdk": "^0.115.0",
    "pdf-lib": "^1.17.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
